interface ButtonProps {
  $variant?: "primary" | "secondary" | "danger";
  $shadow?: "light" | "dark";
  $isSmall?: boolean;
  $isLoading?: boolean;
  $disabled?: boolean;
}

interface IHeaderProps {
  isScrolled: boolean;
}
